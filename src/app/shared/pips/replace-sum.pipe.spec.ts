import { ReplaceSumPipe } from './replace-sum.pipe';

describe('ReplaceSumPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceSumPipe();
    expect(pipe).toBeTruthy();
  });
});
