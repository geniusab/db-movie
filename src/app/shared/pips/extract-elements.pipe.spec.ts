import { ExtractElementsPipe } from './extract-elements.pipe';

describe('ExtractElementsPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractElementsPipe();
    expect(pipe).toBeTruthy();
  });
});
